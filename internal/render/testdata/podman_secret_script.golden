# podman secrets for container solace -- CONTAINS SECRET VALUES.
# Run on this host to create them; `deploy` creates the same ones itself.
printf '%s' 'CHANGE-ME-admin' | podman secret create --replace solace-admin-password -
printf '%s' 'Q0hBTkdFLU1FLXByZXNoYXJlZC1rZXktYmFzZTY0' | podman secret create --replace solace-redundancy-psk -
