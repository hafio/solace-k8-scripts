# docker secrets for container solace -- CONTAINS SECRET VALUES.
# Source this in the shell you run `docker compose` from; `deploy` sets the
# same variables for its own compose process, so it needs nothing from here.
export SOLACE_ADMIN_PASSWORD='pa'\''ss "w" $ord'
export SOLACE_REDUNDANCY_PSK='Q0hBTkdFLU1FLXByZXNoYXJlZC1rZXktYmFzZTY0'
